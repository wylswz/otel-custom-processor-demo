receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  simple: {}

exporters:
  debug: {}

service:
  pipelines:
    metrics:
      receivers: [otlp]
      processors: [simple]
      exporters: [debug]
